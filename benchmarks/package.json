{
  "name": "port-resolver-benchmarks",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Performance benchmarks for port-resolver",
  "scripts": {
    "bench": "node --import tsx internal/operations.bench.ts",
    "bench:internal": "node --import tsx internal/operations.bench.ts",
    "bench:external": "node --import tsx external/get-port.bench.ts && node --import tsx external/detect-port.bench.ts",
    "bench:all": "npm run bench:internal && echo '\n--- External Comparison ---\n' && npm run bench:external"
  },
  "devDependencies": {
    "tatami-ng": "^0.8.18",
    "tsx": "^4.19.2",
    "get-port": "^7.1.0",
    "detect-port": "^1.6.1"
  }
}
